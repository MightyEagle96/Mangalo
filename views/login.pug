h1 Login here 

form(action="/users/login", method="post") 
    div
        label(for="email") Email 
        input#email(type="email", name="email")
    div 
        label(for="password") Password 
        input#pasword(type="password", name="password") 
    div 
        button(type="submit") Login
